module parser::ast;
import lexer;

import std::collections::list;

enum NodeKind : char
{
	ERROR,
	MISSING,

	ATTRIBUTE_DECL,
	CONTRACT_DECL,
	VAR_DECL,
	MODULE_DECL,
	IMPORT_DECL,
	ALIAS_DECL,
	TYPEDEF_DECL,
	FN_DECL,
	PARAM_DECL,

	COMPOUND_STMT,
	IF_STMT,
	CALL_EXPR,

	BINARY_EXPR,
	UNARY_EXPR,
	IDENT_EXPR,
	TYPE_EXPR,
	PATH_EXPR,
	EXPR_LIST,
	LITERAL_EXPR,
	CONTRACT_EXPR,

	CONTRACT_PARAM,
}
